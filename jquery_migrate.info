name = jQuery Migrate
description = Adds jquery migrate plugin to avoid jquery problems with newer versions of jQuery.
package = Administration
core = 7.x
dependencies[] = jquery_update

configure = admin/config/development/jquery-migrate
